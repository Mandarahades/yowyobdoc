spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:development}
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      name: payment-service
      label: master
      uri: ${CONFIG_SERVEUR_URL:http://88.198.150.195:8500}
  security:
    config: 
      bearer: true
      openid-discovery: true

  graphql:
    graphiql:
        enabled: true
        